namespace: observability

commonLabels:
  release: monitoring
  grafana_dashboard: "1"

configMapGenerator:
  - name: gotk-admin-dashboards
    files:
      # GitOps ToolKit
      - gotk/cluster.json
      - gotk/control-plane.json

  - name: istio-admin-dashboards
    files:
      # Istio
      - istio/control-plane.json
      - istio/mesh.json
      - istio/performance.json
      - istio/service.json
      - istio/workload.json