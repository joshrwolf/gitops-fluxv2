namespace: observability

commonLabels:
  release: monitoring
  grafana_dashboard: "1"

configMapGenerator:
  - name: admin-dashboards
    files:
      - gotk/cluster.json
      - gotk/control-plane.json